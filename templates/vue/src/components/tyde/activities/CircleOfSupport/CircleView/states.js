/**
 * Defines the possible states for the CircleToggle component, where the key is the
 * name of the state and the value is that circle's index in the `circles` array.
 *
 * When the state is `All`, all connections are shown. Otherwise, only the
 * connections in the active state is shown.
 */
export const CircleStates = {
  All: 3,
  One: 0,
  Two: 1,
  Three: 2,
}
